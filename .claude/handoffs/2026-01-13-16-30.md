# Session Handoff - 2026-01-13

## Accomplished

### 1. Sorting Options on Filter Bars
- Added `Sort` dropdown to both ArtifactFilterBar and CoinFilterBar
- Options: Name A-Z, Era (Oldest), Era (Newest), Recently Added
- Added `dateAdded` field to Item interface in `lib/items.ts`
- Added `parseYearFromEra()` helper function for date parsing
- Added data attributes (`data-year`, `data-name`, `data-date-added`, `data-tags`) to ItemCard and CoinCard
- Backfilled `dateAdded` to all 5 existing items

### 2. Tag-Based Filtering
- Added `Tag` dropdown to both filter bars
- Created `getAllTags()` function in `lib/items.ts`
- Updated item detail page tags to link to appropriate collection page (coins → /coins, artifacts → /artifacts)
- Tag filter persists in URL params

### 3. Timeline Category Toggles
- Added toggle buttons (Coins, Jewelry, Rings, Seals, Misc) to Timeline.astro
- Added `activeCategories` Set to PixiTimeline class
- Added `toggleCategory()`, `setActiveCategories()`, `getActiveCategories()` public methods
- Modified `update()` loop to filter dots by active categories
- Added CSS for active/inactive toggle states

### 4. Global ⌘K Search Modal
- Created new component: `src/components/SearchModal.astro`
- Keyboard shortcut: ⌘K (Mac) / Ctrl+K (Windows)
- Features: thumbnail previews, keyboard navigation (↑↓ Enter Esc), real-time filtering
- Searches by: name, category, material, period, tags
- Included in BaseLayout

### 5. OG Images + RSS Feed
- Item pages now pass first image to BaseLayout for Open Graph meta tags
- Created RSS endpoint: `src/pages/rss.xml.ts`
- Added RSS discovery link in BaseLayout head
- Installed `@astrojs/rss` package

### 6. Print-Friendly CSS
- Added comprehensive `@media print` styles to global.css
- Hides: matrix bg, header, footer, nav, glass panels, buttons
- White background, black text
- Single-column layout, page break handling

## Key Files Modified

| File | Changes |
|------|---------|
| `src/lib/items.ts` | Added `dateAdded`, `parseYearFromEra()`, `getAllTags()` |
| `src/components/ArtifactFilterBar.astro` | Added Tag/Sort dropdowns, `filterAndSort()` logic |
| `src/components/CoinFilterBar.astro` | Added Tag/Sort dropdowns, `filterAndSort()` logic |
| `src/components/ItemCard.astro` | Added data attributes for sorting |
| `src/components/CoinCard.astro` | Added data attributes for sorting |
| `src/pages/item/[id].astro` | Updated tag links, added OG image prop |
| `src/lib/timeline/pixi-timeline.ts` | Added category filtering methods |
| `src/components/timeline/Timeline.astro` | Added category toggle buttons UI |
| `src/components/SearchModal.astro` | NEW - global search modal |
| `src/pages/rss.xml.ts` | NEW - RSS feed endpoint |
| `src/layouts/BaseLayout.astro` | Added SearchModal, RSS link |
| `src/styles/global.css` | Added print styles |
| `src/data/items/*.yaml` | Added `dateAdded` field to all items |

## Current State

- Build passes successfully
- All 6 features implemented and integrated
- Pagefind indexes 5 pages
- RSS feed available at `/rss.xml`

## Next Steps

1. **Test in browser** - Verify all features work as expected:
   - Sort/filter on /artifacts and /coins pages
   - ⌘K search from any page
   - Timeline category toggles
   - Print view on item pages
   - RSS feed validation

2. **Potential enhancements** (from original list):
   - Statistics page (collection breakdown by era, material, category)
   - Bibliography linking (connect books to items)
   - Keyboard navigation between items (arrow keys on detail pages)

3. **Content additions**:
   - Add more items to the collection
   - Ensure all items have proper `dateAdded` values

## Blockers

None currently.

## Key Decisions

1. **Sort dropdown placement** - After filter dropdowns, before item count
2. **Tag links** - Navigate to appropriate collection page based on item category (coin vs artifact)
3. **Timeline filtering** - Items moved off-screen (x=-1000) when category hidden, rather than visibility toggle
4. **Search modal** - Uses inline JSON of all items for instant search (no server round-trip)
5. **Print styles** - Hide all interactive/decorative elements, white bg for readability
6. **dateAdded format** - ISO date strings (YYYY-MM-DD) for proper sorting
