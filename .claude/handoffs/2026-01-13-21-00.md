# Session Handoff - 2026-01-13 21:00

## Accomplished

### Articles Page Enhancements
- Added RSS feed link to articles page header (next to description, with RSS icon)
- Implemented tag-based filtering with clickable chip buttons
- Filter bar styled as glass panel matching coins/artifacts filter bars
- Added horizontally scrollable tag container with mouse wheel support
- Shows article count that updates when filtering
- URL persistence for selected tag (`?tag=coins`)
- Clear button appears when filter is active

### Landing Page Vignette Effect
- Added subtle dimming gradients at top and bottom edges of viewport
- Top vignette: Hidden at page top, fades in after scrolling 50px
- Bottom vignette: Visible while scrolling, fades out near page bottom
- Prevents obscuring navigation header and footer

### CSS Utilities
- Added `.scrollbar-hide` utility to global.css for hiding scrollbars while maintaining scroll functionality

## Key Files Modified

- `archive/src/pages/library/articles/index.astro` - RSS link, tag filtering, horizontal scroll
- `archive/src/pages/index.astro` - Vignette overlays with scroll-aware visibility
- `archive/src/styles/global.css` - scrollbar-hide utility

## Current State

- All features working as expected
- Tag filtering persists in URL and works on page load
- Vignettes fade in/out appropriately based on scroll position
- Horizontal scroll on tag bar works with mouse wheel (converts vertical to horizontal)

## Next Steps

1. Consider adding tag filtering to individual article pages (related articles by tag)
2. Could add RSS auto-discovery link in `<head>` for better RSS reader support
3. Potentially extend vignette effect to other long pages if desired
4. Consider adding sort options to articles (by date, alphabetically)

## Blockers

None

## Key Decisions

- **Chip-style tags vs dropdown**: Chose clickable chips over dropdown for articles since the tag count is manageable and provides better UX for quick filtering
- **Vignette thresholds**: 50px threshold for showing/hiding vignettes provides smooth transition without obscuring header/footer
- **scrollBy with smooth behavior**: Used `scrollBy({ behavior: 'smooth' })` for horizontal scroll instead of direct `scrollLeft` assignment for smoother UX
